# macOS
.DS_Store
.AppleDouble
.LSOverride

# Node
node_modules/

# 环境变量（含密钥，绝对不提交）
.env
server/.env

# 上传的文件（体积大，走服务器本地存储）
server/uploads/

# 微信小程序（本地开发，通过微信开发者工具直接发布，无需部署到服务器）
weichat/
project.config.json
project.private.config.json

# 日志
*.log
logs/

# IDE
.idea/
.vscode/
*.swp

# Claude Code 本地配置
.claude/

# 本地归档与清理备份（不进入仓库）
.local-archive/

# Codex/agent 本地任务记录（已在本机使用，不进入仓库）
tasks/
